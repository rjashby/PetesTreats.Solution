@{
  Layout = "_Layout";
}

@using PetesTreats.Models;

<h1>Treats Matching Your Search</h1>

@if (@Model.Count == 0)
{
  <h3>No treats were found!</h3>
} 

<ul>
  @foreach(Treat treat in Model)
  {
    <li>@Html.ActionLink($"{treat.Name}", "Details", new { id = treat.TreatID })</li>
  }
</ul>
